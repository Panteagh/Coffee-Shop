export interface Children {
  children: React.ReactNode;
}

export interface Product{
    price: string
    name: string
    image: string
    description: string
    category: string
    id: string
  }